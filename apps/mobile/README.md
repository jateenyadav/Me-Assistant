# @lifeos/mobile (placeholder)

The Flutter app (Android + iOS) is scaffolded in the **Phase 1 mobile-capture slice**, when
the Android `NotificationListenerService` pipeline is built. Phase 1's manual transaction
API and web dashboard have already started; this directory remains a placeholder. See
[`docs/memory/06_ROADMAP.md`](../../docs/memory/06_ROADMAP.md).

Nothing here yet by design — backend/manual entry comes before mobile notification capture.
