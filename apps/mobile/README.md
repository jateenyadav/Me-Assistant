# @lifeos/mobile (placeholder)

The Flutter app (Android + iOS) is scaffolded in **Phase 1** (finance module), when the
Android `NotificationListenerService` pipeline is built. See
[`docs/memory/06_ROADMAP.md`](../../docs/memory/06_ROADMAP.md).

Nothing here yet by design — Session 0 ships the backend + web foundation first.
