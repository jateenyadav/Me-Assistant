# Only enable the Google button when the API's Google OAuth credentials are set.
NEXT_PUBLIC_GOOGLE_AUTH_ENABLED=false
# API base URL, reachable from the user's browser.
NEXT_PUBLIC_API_URL=http://localhost:4000
